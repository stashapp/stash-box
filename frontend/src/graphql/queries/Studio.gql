query Studio($id: ID!) {
  findStudio(id : $id) {
    id
    name
    parent {
      id
      name
    }
    urls {
        url
        type
    }
    images {
        id
        url
        height
        width
    }
  }
}
