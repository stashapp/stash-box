query Users($input: UserQueryInput!) {
  queryUsers(input: $input) {
    count
    users {
      id
      name
      email
      roles
      invited_by {
        id
        name
      }
      invite_tokens
    }
  }
}
