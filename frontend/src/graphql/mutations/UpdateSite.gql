mutation UpdateSite($siteData: SiteUpdateInput!) {
  siteUpdate(input: $siteData) {
    id
    name
    description
    url
    regex
    valid_types
    category {
      id
      name
      sort_order
    }
    highlighted
  }
}
