scalar Upload

type Image {
  id: ID!
  url: String!
  width: Int
  height: Int
}

input ImageCreateInput {
  url: String
  file: Upload
}

input ImageUpdateInput {
  id: ID!
  url: String
  file: Upload
}

input ImageDestroyInput {
  id: ID!
}
