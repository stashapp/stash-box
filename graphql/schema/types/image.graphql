scalar Upload

type Image {
  id: ID!
  url: String!
  width: Int!
  height: Int!
}

input ImageCreateInput {
  file: Upload
}

input ImageUpdateInput {
  id: ID!
}

input ImageDestroyInput {
  id: ID!
}
